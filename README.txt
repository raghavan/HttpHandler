A simple java sample class to make use of the apache httpcomponent library to make GET/POST calls


Note:
Apache httpcomponent jar can be downloaded from http://hc.apache.org/downloads.cgi
and add in your project's build path;